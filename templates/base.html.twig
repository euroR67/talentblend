<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css" integrity="sha512-z3gLpd7yknf1YoNbCzqRKc4qyor8gaKU1qmn+CShxbuBusANI9QpRohGBreCFkKxLhei6S9CQXFEbbKuqLg0DA==" crossorigin="anonymous" referrerpolicy="no-referrer" />
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
        <link rel="stylesheet" href="{{asset('css/style.css')}}">
        {% block stylesheets %}
            <link rel="stylesheet" href="{{ asset('styles/app.css') }}">
        {% endblock %}
    </head>
    <body>
        <header>
            <nav>
                <ul>
                    <li>
                        <i class="fa-solid fa-bars" style="color: #007456; cursor: pointer;"></i>
                        <img class="logo" src="{{ asset('img/logo.png') }}" alt="logo">
                    </li>
                    <li><i class="fa-solid fa-magnifying-glass" style="color: #5d5d5d; cursor: pointer;"></i></li>
                </ul>
            </nav>
            <nav class="slide-menu">
                <div>
                    {# Afficher un lien pour se connecter si l'utilisateur n'est pas connecté #}
                    {% if not app.user %}
                        <span>
                            <a href="{{ path('app_login') }}">Se connecter</a>
                            <a href="{{ path('app_register') }}">S'inscrire</a>
                        </span>
                    {% endif %}
                    {# Afficher la photo de profil de l'utilisateur et son nom s'il est connecté #}
                    {% if app.user %}
                        <span>
                            <img src="{{ asset('img/profil.jpg') }}" alt="avatar">
                            {{ app.user.prenom }} {{ app.user.nom }}
                        </span>
                    {% endif %}
                    <i class="fa-solid fa-xmark" style="color: #666666; cursor: pointer;"></i>
                </div>
                <ul>
                    {% if app.user %}
                        <li><a href="{{ path('app_candidat_edit', {'id': app.user.id}) }}">Mon profil</a></li>
                    {% endif %}
                    <li>Accueil</li>
                    <li class="categorie">Catégorie <i class="fa-solid fa-chevron-down fa-xs"></i></li>
                    <li class="sub-menu">
                        <ul>
                            <li>Informatique & Technologie</li>
                            <li>Batiment</li>
                            <li>Commercial</li>
                        </ul>
                    </li>
                    <li>Offres d'emploi</li>
                    <li>Déposer CV</li>
                    {% if app.user %}
                        <li><a href="{{ path('app_logout') }}">Déconnexion</a></li>
                    {% endif %}
                    <li><a class="post-offre" href="">Poster une offre</a></li>
                </ul>
            </nav>
            <div class="overlay"></div>
        </header>
        <div class="bg"></div>
        {% block body %}{% endblock %}
        <script src="{{ asset('js/script.js') }}"></script>
    </body>
</html>
